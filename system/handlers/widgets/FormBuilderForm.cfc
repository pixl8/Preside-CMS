component {

	private function index( event, rc, prc, args={} ) {
		return "";
	}

	private string function placeholder( event, rc, prc, args={} ) {
		return "Form builder form (TODO, change placeholder)";
	}
}