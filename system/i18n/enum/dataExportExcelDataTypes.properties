mapped.label=Map data types to Excel (default)
mapped.description=Data types will be mapped to equivalent Excel data types (e.g. numeric values will be numeric, dates will be dates)

string.label=Export all as string
string.description=All values will be treated as strings by Excel. This is the legacy behaviour; use this if you have backward-compatibility issues