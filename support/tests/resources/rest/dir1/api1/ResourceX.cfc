/**
 * @restUri /test/{pattern},/test/{pattern}/{id}/
 *
 */
component {

	private void function get( event, rc, prc, args, resp ) {
		return;
	}

	private void function delete( event, rc, prc, args, resp ) {
		return;
	}

	private void function putDatatest( event, rc, prc, args, resp ) restVerb="put" {
		return;
	}

}